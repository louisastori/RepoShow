{
  "name": "reposhow",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Generate a living developer portfolio from GitHub repositories.",
  "scripts": {
    "validate": "node ./src/validate-example.mjs",
    "test": "node --test test/manifest-validator.test.mjs",
    "coverage": "node --test --experimental-test-coverage test/manifest-validator.test.mjs",
    "check": "npm run validate && npm run test && npm run coverage",
    "quality:ai": "powershell -NoProfile -ExecutionPolicy Bypass -File ./scripts/run-ai-quality.ps1"
  },
  "engines": {
    "node": ">=20"
  }
}
